describe "PopulrAssetCollector", () ->
  populrAnalyticsCollector = null
  windowProxy = null

  beforeEach () ->
    windowProxy = {}
    windowProxy.

  describe "#not_signed_in", () ->
    it "should call end_call", () ->
    it "should display notification and a button that a user can click to open a new tab for signing in to populr.me", () ->

  describe "#add_asset", () ->
    it "should call start_call, passing it the current asset dom id", () ->
    it "should call the tunnel", () ->
